<Window x:Class="PlayNow.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PlayNow" Icon="/app.ico" Height="920" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border CornerRadius="8" Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1">
                                            <Path x:Name="Arrow" Fill="{DynamicResource TextPrimary}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="{DynamicResource WindowBackground}" BorderThickness="1" BorderBrush="{DynamicResource ButtonBorder}">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBackground}" CornerRadius="20" BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}" CornerRadius="20,20,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <TextBlock Text="⚡ PlayNow Client" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                        <TextBlock x:Name="LicenseStatsTxt" Text="" Foreground="#00FA9A" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="11" FontWeight="Bold"/>
                        <Button x:Name="MiniModeBtn" Content="📱 Мини-режим" Style="{StaticResource GlassBtn}" Width="100" Height="28" Margin="0,0,10,0" FontSize="11" Click="MiniModeBtn_Click"/>
                        <Button x:Name="SupportBtn" Content="💬 Telegram Support" Style="{StaticResource GlassBtn}" Width="120" Height="28" Margin="0,0,10,0" FontSize="11" Click="SupportBtn_Click"/>
                        <Button Content="―" Style="{StaticResource GlassBtn}" Width="35" Height="28" Click="MinimizeBtn_Click"/>
                        <Button Content="✕" Style="{StaticResource GlassBtn}" Width="35" Height="28" Foreground="#FF4A4A" Click="CloseBtn_Click" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="LeftColumn" Width="310"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="LeftPanel" Grid.Column="0" Background="{DynamicResource PanelBackground}" CornerRadius="20" Margin="0,0,20,0" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Серверы" Foreground="{DynamicResource TextPrimary}" FontSize="18" FontWeight="Bold" Margin="0,0,0,5" HorizontalAlignment="Center"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                            <Button Content="📶 Пинг" Style="{StaticResource GlassBtn}" Width="60" Height="25" FontSize="10" Margin="2" Click="SortPing_Click"/>
                            <Button Content="🔤 Имя" Style="{StaticResource GlassBtn}" Width="60" Height="25" FontSize="10" Margin="2" Click="SortName_Click"/>
                            <Button Content="👥 Люди" Style="{StaticResource GlassBtn}" Width="60" Height="25" FontSize="10" Margin="2" Click="SortLoad_Click"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden">
                            <StackPanel x:Name="ServerListContainer"/>
                        </ScrollViewer>

                        <Button Grid.Row="3" Style="{StaticResource AccentBtn}" Content="🔍 ОБНОВИТЬ СПИСОК" Height="40" Margin="0,15,0,0" Click="ScanBtn_Click"/>
                        <Button Grid.Row="4" Content="📊 ПОКАЗАТЬ НАГРУЗКУ" Style="{StaticResource GlassBtn}" Height="35" Margin="0,10,0,0" Click="ToggleLoadBtn_Click"/>
                    </Grid>
                </Border>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel x:Name="TabsPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <Button x:Name="BtnTabConnect" Style="{StaticResource AccentBtn}" Content="⚡ Подключение" Width="155" Height="40" Click="TabConnect_Click"/>
                        <Button x:Name="BtnTabSystem" Style="{StaticResource GlassBtn}" Content="🛠️ Система" Width="155" Height="40" Click="TabSystem_Click" Margin="5,0,0,0"/>
                    </StackPanel>

                    <Grid x:Name="PageConnection" Grid.Row="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border x:Name="NewsPanel" Grid.Row="0" Background="#33FFD700" CornerRadius="12" Margin="0,0,0,15" Padding="10" Visibility="Collapsed">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="🔔" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="NewsText" Text="Загрузка новостей..." Foreground="#FFD700" FontWeight="Bold" FontSize="13" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="1" Background="{DynamicResource PanelBackground}" CornerRadius="20" Padding="20">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock x:Name="StatusIcon" Text="●" Foreground="#FF4A4A" FontSize="50" HorizontalAlignment="Center" Margin="0,-15,0,-5"/>
                                    <TextBlock x:Name="StatusText" Text="ОТКЛЮЧЕНО" Foreground="{DynamicResource TextPrimary}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="StatusInfo" Text="Выберите игры и сервер" Foreground="{DynamicResource TextSecondary}" FontSize="12" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="2" Background="{DynamicResource PanelBackground}" CornerRadius="20" Height="85" Margin="0,20,0,0">
                                <Grid>
                                    <Canvas x:Name="PingCanvas" VerticalAlignment="Bottom" Height="65" Margin="5"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="15,8">
                                        <TextBlock x:Name="AvgPingTxt" Text="AVG: 0 ms" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,12,0"/>
                                        <TextBlock x:Name="MaxPingTxt" Text="MAX: 0 ms" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                                    </StackPanel>
                                    <TextBlock x:Name="CurrentPingTxt" Text="" Foreground="#00FA9A" FontWeight="Bold" FontSize="16" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="15,8"/>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Row="4" VerticalAlignment="Bottom">

                                <StackPanel x:Name="TunnelInfoPanel" Margin="0,15,0,0">

                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="Активные приложения:" Foreground="{DynamicResource TextSecondary}" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock x:Name="ActiveAppsTxt" Text="Ничего не выбрано" Foreground="{DynamicResource TextPrimary}" FontSize="13" FontWeight="Bold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>

                                        <StackPanel x:Name="TechInfoPanel" Orientation="Horizontal" Margin="0,6,0,0" Visibility="Collapsed">
                                            <Border Background="#204A90E2" CornerRadius="4" Padding="4,2" Margin="0,0,5,0">
                                                <TextBlock x:Name="TechIpTxt" Text="IP: 0.0.0.0" Foreground="#4A90E2" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                            <Border Background="#2000CA71" CornerRadius="4" Padding="4,2">
                                                <TextBlock x:Name="TechProtoTxt" Text="VLESS Reality" Foreground="#00CA71" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Background="#0AFFFFFF" CornerRadius="8" BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1" Padding="10,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                                <TextBlock Text="☁️ Обход блокировок (CDN)" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" FontSize="13"/>
                                                <TextBlock Text="(включать только по необходимости)" Foreground="#FFD700" FontSize="10" Margin="0,3,0,0" TextWrapping="Wrap" Opacity="0.8"/>
                                            </StackPanel>

                                            <ToggleButton x:Name="CdnToggle" Style="{StaticResource ToggleSwitch}" IsChecked="False" VerticalAlignment="Center" Height="26" Grid.ColumnSpan="2" Margin="395,0,5,0"/>
                                        </Grid>
                                    </Border>
                                </StackPanel>

                                <StackPanel x:Name="ExtraToolsPanel">
                                    <Grid Margin="0,20,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="DNS:" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <ComboBox x:Name="DnsSelector" Grid.Column="1" Height="35" Style="{StaticResource DarkComboBox}">
                                            <ComboBoxItem Content="System" IsSelected="True" Foreground="{DynamicResource TextPrimary}"/>
                                            <ComboBoxItem Content="Google" Foreground="{DynamicResource TextPrimary}"/>
                                            <ComboBoxItem Content="Cloudflare" Foreground="{DynamicResource TextPrimary}"/>
                                        </ComboBox>
                                        <ToggleButton x:Name="BoostToggle" Grid.Column="2" Style="{StaticResource ToggleSwitch}" Content="🚀 Boost FPS" IsChecked="True" Margin="10,0,0,0"/>
                                    </Grid>

                                    <Button Content="🎮 Выбор приложений (Split)" Style="{StaticResource GlassBtn}" Height="50" FontSize="15" FontWeight="Bold" Margin="0,0,0,10" Click="AppsBtn_Click"/>
                                    <Button Content="🛡️ Проверка доступности сервисов" Style="{StaticResource GlassBtn}" Height="50" FontSize="15" FontWeight="Bold" Margin="0,0,0,10" Click="CheckSitesBtn_Click"/>
                                    <Button Content="⏱ Speedtest (iPerf3)" Style="{StaticResource GlassBtn}" Height="50" FontSize="15" FontWeight="Bold" Margin="0,0,0,20" Click="SpeedtestBtn_Click"/>
                                </StackPanel>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="⚡ ПОДКЛЮЧИТЬ" Style="{StaticResource AccentBtn}" Height="55" FontSize="16" FontWeight="Bold" Click="ConnectBtn_Click"/>
                                    <Button Grid.Column="2" Content="⛔ ОТКЛЮЧИТЬ" Style="{StaticResource GlassBtn}" Height="55" FontSize="16" FontWeight="Bold" Foreground="#FF5A5A" Click="DisconnectBtn_Click"/>
                                </Grid>
                                <Button x:Name="ReportBtn" Content="🐞 Сообщить о проблеме" Style="{StaticResource GlassBtn}" Height="35" Margin="0,10,0,0" Opacity="0.6" Click="ReportBtn_Click"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <Grid x:Name="PageSystem" Grid.Row="1" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="10,0,10,20">

                                <Button x:Name="UpdateBtn" Visibility="Collapsed" Style="{StaticResource AccentBtn}" Height="50" Margin="0,0,0,15" Click="UpdateBtn_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⬇" FontSize="16" Margin="0,0,10,0"/>
                                        <TextBlock Text="ДОСТУПНА НОВАЯ ВЕРСИЯ" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <TextBlock Text="🖥️ Экран и Видео" Foreground="{DynamicResource TextAccent}" FontWeight="Bold" FontSize="16" Margin="0,10,0,10"/>
                                <UniformGrid Columns="2" Margin="0,0,0,15">
                                    <Button Content="🟢 NVIDIA Panel" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="OpenNvidia_Click"/>
                                    <Button Content="⚡ Максимальная Герцовка" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="MaxRefreshRate_Click"/>
                                </UniformGrid>

                                <TextBlock Text="🎙️ Аудио и Микрофон" Foreground="{DynamicResource TextAccent}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                    <Button Content="🔊 Звук Windows" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="OpenSoundSettings_Click"/>
                                    <Button Content="🎤 Тест Микрофона" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="TestMic_Click"/>
                                </UniformGrid>
                                <ProgressBar x:Name="MicLevelBar" Height="10" Margin="5,0,5,15" Minimum="0" Maximum="100" Value="0" Foreground="#00CA71" Background="#222" BorderThickness="0"/>

                                <TextBlock Text="⚙️ Дополнительно" Foreground="{DynamicResource TextAccent}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                    <Button Content="🖱️ Параметры Мыши" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="OpenMouseSettings_Click"/>
                                    <Button Content="🔋 Режим Питания" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="OpenPowerSettings_Click"/>
                                </UniformGrid>

                                <TextBlock Text="🎨 Внешний вид" Foreground="{DynamicResource TextAccent}" FontWeight="Bold" FontSize="16" Margin="0,10,0,10"/>
                                <UniformGrid Columns="2" Margin="0,0,0,10">
                                    <Button Content="🌗 Сменить тему" Style="{StaticResource GlassBtn}" Height="55" Margin="5" Click="ChangeTheme_Click"/>
                                </UniformGrid>

                                <TextBlock Text="PlayNow Client v2.0.1" Foreground="{DynamicResource TextSecondary}" FontSize="10" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Grid>

            <Grid x:Name="NotificationOverlay" Grid.RowSpan="2" Visibility="Collapsed" Panel.ZIndex="2000">
                <Border Background="#CC000000" MouseDown="Notification_CloseClick"/>
                <Border Width="420" Background="{DynamicResource WindowBackground}" CornerRadius="18" BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="25">
                    <StackPanel>
                        <TextBlock x:Name="NotifTitle" Text="Заголовок" Foreground="{DynamicResource TextPrimary}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock x:Name="NotifMessage" Text="Текст уведомления..." Foreground="{DynamicResource TextSecondary}" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"/>
                        <Button Content="ПОНЯТНО" Style="{StaticResource AccentBtn}" Height="45" FontSize="14" Click="Notification_CloseClick"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid x:Name="ReportOverlay" Grid.RowSpan="2" Visibility="Collapsed" Panel.ZIndex="2000">
                <Border Background="#E6000000" MouseDown="Report_CancelClick"/>
                <Border Width="450" Background="{DynamicResource WindowBackground}" CornerRadius="18" BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20">
                    <StackPanel>
                        <TextBlock Text="🐞 Сообщить о проблеме" Foreground="#FF4A4A" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="Опишите проблему, чтобы мы могли её решить:" Foreground="{DynamicResource TextSecondary}" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBox x:Name="ReportCommentBox" Height="100" Background="#20FFFFFF" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" Padding="10" TextWrapping="Wrap" AcceptsReturn="True" FontSize="14" VerticalScrollBarVisibility="Auto">
                            <TextBox.Resources>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="ОТМЕНА" Style="{StaticResource GlassBtn}" Height="45" Click="Report_CancelClick"/>
                            <Button Grid.Column="2" Content="ОТПРАВИТЬ" Style="{StaticResource AccentBtn}" Height="45" Click="Report_SendClick"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid x:Name="ActivationOverlay" Grid.RowSpan="2" Visibility="Collapsed" Panel.ZIndex="1000">
                <Border Background="{DynamicResource WindowBackground}" CornerRadius="20">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="🔒 АКТИВАЦИЯ" Foreground="{DynamicResource TextAccent}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock x:Name="ClubIdTxt" Text="ID Клуба: ..." Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                        <TextBox x:Name="KeyInput" Width="330" Height="50" Background="#15FFFFFF" Foreground="{DynamicResource TextPrimary}" BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1" FontSize="18" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <TextBlock x:Name="ActivationStatusTxt" Text="" Foreground="#FF4500" HorizontalAlignment="Center" Margin="0,15,0,20"/>
                        <Button Content="АКТИВИРОВАТЬ" Style="{StaticResource AccentBtn}" Width="260" Height="55" FontSize="16" Click="ActivateBtn_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>